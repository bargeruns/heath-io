import { state } from './orderbook.state';
import { actions } from './orderbook.actions';
import { mutations } from './orderbook.mutations';

export const orderbook = {
  state,
  actions,
  mutations
};